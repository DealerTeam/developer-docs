# 
version: 2.0
jobs:
  generate-docs-job:
    docker:
      - image: cimg/node:15.0.1
    steps:
      - checkout   # Required step.  Checks out the code from your repository.
      - run:
          name:  display some info
          command: |
            node -v
            pwd
            ls -al
      - run:
          name: install Node package @cparra/apexdocs
          command: |
            npm update
            sudo npm install -g @cparra/apexdocs --no-package-lock
      - run:
          name: run apexdocs node package/command to generate docs
          command: |
            pwd
            mkdir outputdocs
            mkdir classcopy
            cp classes/* classcopy
            ls -la
            dirname classcopy
            readlink -f classcopy
            apexdocs-generate -s /home/circleci/project/classcopy -t /home/circleci/project/outputdocs2
  
workflows:
  version: 2
  build_and_test:
    jobs:
      - generate-docs-job:
          context: org-global        
#END-OF-CONFIG-FILE#
#DEALERTEAM.COM RELEASE ENGINEERING#
##